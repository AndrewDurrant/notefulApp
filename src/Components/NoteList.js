import React, { Component } from 'react'

export class NoteList extends Component {
  render() {
    return (
      <div>
        // map over the store and and build each list:

        {/* <Note /> */}
        <button>Add Note</button>
      </div>
    )
  }
}

export default NoteList
